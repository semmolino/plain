{# Branding-<style> (Schrift-Einbettung + Akzentfarbe + Logo-Position).
   Wird zentral in JS gebaut (services_pdf_render.js -> buildThemeHead) und als
   vm.themeHead uebergeben — so kann die gewaehlte Schrift base64-eingebettet
   werden. Am Ende des <head> eingebunden, damit diese Regeln gewinnen. #}
{{ themeHead | safe }}
